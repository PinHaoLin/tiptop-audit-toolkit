# Design

## Prompt Ownership

交付提示屬於工作流程交付資訊，放置在左側主工作區；右側只負責顯示後端連線、資料夾狀態與路徑。

## Height

`.primary-panel` 使用內容高度排列，交付提示不使用 `height: 100%` 或 `1fr` 撐滿剩餘空間。提示卡保留正常 Card 內距，文字可依視窗寬度換行。

## Verification

- 桌面版只顯示一個交付提示。
- 左側交付提示不延伸到右側執行環境卡的高度。
- 平板與手機版仍維持單欄堆疊且不產生水平溢位。
